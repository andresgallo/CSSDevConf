#!/usr/bin/env ruby
require 'fileutils'

# unix "touch" any file names passed to the script

if ARGV.empty? then
    puts "Usage: #{$0} FILES"
    puts "mtouch is used to force update the specified files' timestamp."
    exit
end

ARGV.each do |filename|
    FileUtils.touch(filename)
end
